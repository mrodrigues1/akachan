{
  "name": "akachan-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"vitest run --no-file-parallelism\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0",
    "vitest": "^2.0.0"
  }
}
